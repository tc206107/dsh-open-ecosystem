# 贡献指南

感谢你愿意为 dsh-open-ecosystem 贡献。请遵循以下约定，保证仓库可维护、可追溯。

## 工作流

1. **先讨论后编码**：涉及行为变更 / API 形状 / 兼容范围的改动，先开 issue 说明动机与影响面；
   纯 bug 修复、文档修正可直提 PR。
2. **Fork + 分支**：从 `master` 切 `fix/...` 或 `feat/...` 分支；PR 标题遵循
   `fix:`, `feat:`, `docs:`, `chore:` 前缀。
3. **验证**：合并前必须通过
   - JS 组件：`node --check` 语法检查；若有单测则 `npm test`；
   - PowerShell 脚本：以 UTF-8 读取做语法解析（避免 ANSI 误判）；
   - 行为冒烟：改动涉及运行时逻辑时，附最小复现 / 验证记录。
4. **兼容性声明**：如涉及 `@deepseek-ai/*` 版本或 DSH mainline commit，在 PR 描述中标注
   已验证的版本/commit，便于跟踪破坏性变更。

## 代码规范

- **不引入个人信息**：禁止提交真实用户名、邮箱、本机绝对路径、API 密钥；路径一律环境变量
  化（见 README 环境变量约定表），个人路径用 `<PLACEHOLDER>` 占位。
- **纯集成层**：不修改 DSH 源码；插件/脚本以挂载或封装方式工作，卸载可还原。
- **幂等与可回滚**：新增脚本应可重复执行且不留副作用；配置类改动给出还原步骤。
- **第三方内容**：调研材料保留原 LICENSE 与署名；优先 fork 或依赖引用，不搬代码。

## 许可

提交即表示同意以 MIT 许可发布你的贡献（见 [LICENSE](./LICENSE)）。
